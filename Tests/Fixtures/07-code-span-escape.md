The formula `=apfel(...)` is documentation, not a call.
But =math(2+2) really is a call.
Another escaped form: `=(hello world)` should not evaluate.
Third: `=math(1+1)` is also escaped.
Final: =math(10/2) evaluates for real.
